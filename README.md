# TO RUN:

1. Install [nodejs](https://nodejs.org/en/download/)
2. In the project folder, run `npm i`
3. In the project folder, run `node .`
4. Open a web browser and navigate to `localhost:8080`
5. Enjoy!
